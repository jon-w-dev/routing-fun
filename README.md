Super basic react router setup for the lecture.

run `git clone`
 to get it locally

run `pnpm || npm || yarn install`
 to get the packages installed

run `pnpm || npm || yarn run dev`
  to get the project running
